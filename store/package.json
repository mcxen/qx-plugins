{
  "name": "qx-plugin-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:data": "node scripts/prepare.mjs",
    "dev": "npm run prepare:data && vite",
    "build": "npm run prepare:data && vite build",
    "preview": "vite preview",
    "pages:dev": "npm run build && wrangler pages dev dist --port 8788",
    "pages:deploy": "npm run build && wrangler pages deploy dist --project-name=qx-plugin-store"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "wrangler": "^4.20.0"
  }
}
